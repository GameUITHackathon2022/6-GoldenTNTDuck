import { createTheme } from '@mui/material/styles';
const theme = createTheme({
    palette: {
      secondary: {
        light: '#a3e635',
        main: '#65a30d',
        dark: '#65a30d',
        contrastText: '#000',
      },
    },
  });

export default theme